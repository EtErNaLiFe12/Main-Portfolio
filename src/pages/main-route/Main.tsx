import { Box, Typography } from '@mui/material';

export default function Main() {
  return (
    <Box sx={{ mt: 5, textAlign: 'center', color: '#000' }}>
      <Typography>React Boiler Template</Typography>
    </Box>
  );
}
